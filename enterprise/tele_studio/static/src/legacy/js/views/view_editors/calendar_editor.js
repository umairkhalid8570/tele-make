tele.define('tele_studio.CalendarEditor', function (require) {
"use strict";

var CalendarRenderer = require('web.CalendarRenderer');

var EditorMixin = require('tele_studio.EditorMixin');

return CalendarRenderer.extend(EditorMixin, {
});

});
